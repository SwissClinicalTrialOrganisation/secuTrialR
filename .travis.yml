services:
  - xvfb

before_install:
 - export DISPLAY=:99.0
 - sh -e /etc/init.d/xvfb start

language: r

sudo: required

warnings_are_errors: true

apt_packages:
  - tcl8.5-dev
  - tk8.5-dev

r_packages:
  - covr

after_success:
  - Rscript -e 'library(covr); codecov()'
